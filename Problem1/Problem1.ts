interface BintreeNode {
    id: string | number,
    left: BintreeNode,
    right: BintreeNode
}

let input = ["a", ["b"], ["c"]];


function convertion(data:Array<any>):void {

}


convertion(input);